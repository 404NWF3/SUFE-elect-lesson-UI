# 更新日志

## 版本 2.0 - 2026-01-13

### 新增功能

1. **自动获取课程详细信息**
   - 在添加选/退课任务时，系统会自动从选课系统获取课程的详细信息
   - 包括课程名称和教师信息
   - 使用URL: `https://eams.sufe.edu.cn/eams/allTeachTaskSearch!info.action?removeBack=1&lesson.id={课程ID}`

2. **优化的UI显示**
   - 任务列表中显示课程ID和课程名称的简短版本
   - 课程名称超过20个字符时自动截断，后面显示"..."
   - 支持鼠标悬停显示完整的课程信息（包括课程ID、课程名称、教师）

3. **增强的日志功能**
   - 添加任务时记录课程详细信息
   - 更清晰的任务添加日志输出

### 技术实现

#### 1. HTML解析方法
新增 `get_lesson_detail_from_html(lesson_id)` 方法：
- 使用正则表达式解析HTML响应
- 提取课程名称和教师信息
- 自动处理HTML实体（如 `&nbsp;`）

#### 2. Tooltip提示功能
- 使用tkinter的Toplevel窗口实现悬停提示
- 绑定`<Motion>`和`<Leave>`事件
- 显示完整的课程ID、名称和教师信息

#### 3. 任务数据结构更新
任务列表数据结构从：
```python
{
    'elect_id': str,
    'withdraw_id': str,
    'status': str
}
```

更新为：
```python
{
    'elect_id': str,
    'withdraw_id': str,
    'status': str,
    'elect_name': str,        # 新增：要选课程的名称
    'elect_teacher': str,     # 新增：要选课程的教师
    'withdraw_name': str,     # 新增：要退课程的名称
    'withdraw_teacher': str   # 新增：要退课程的教师
}
```

### 使用说明

1. **添加任务**
   - 点击"添加任务"按钮
   - 输入要选的课程ID（必填）
   - 如果是换课，还需输入要退的课程ID
   - 点击"确定"后，系统会自动获取课程详细信息

2. **查看课程详细信息**
   - 方式1：在任务列表中直接看到课程ID和名称（简短版）
   - 方式2：将鼠标悬停在任务行上，会弹出完整的课程信息提示框

### 示例

添加任务后的显示效果：
```
要选的课程: ID: 2186 - 当代英语短篇小说名作赏析
要退的课程: 无
状态: 等待中
```

鼠标悬停时的提示框：
```
【要选的课程】
ID: 2186
课程名称: 当代英语短篇小说名作赏析
教师: 宋明
```

### 测试

运行测试脚本验证HTML解析功能：
```bash
python test_html_parse.py
```

### 兼容性说明

- 保持了向后兼容，旧版本导出的配置文件可以正常导入
- 如果课程详细信息获取失败，不会影响任务的添加，只是不显示名称和教师信息
